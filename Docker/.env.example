# Copy this file to Docker/.env for local development.
# Docker/.env is ignored by the repository's .gitignore.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=replace-with-a-strong-local-secret
POSTGRES_DB=postgres
